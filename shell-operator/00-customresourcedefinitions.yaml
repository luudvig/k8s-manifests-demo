apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: rotatingconfigs.stable.example.com
spec:
  group: stable.example.com
  versions:
  - name: v1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              values:
                type: array
                items:
                  type: string
  scope: Namespaced
  names:
    plural: rotatingconfigs
    singular: rotatingconfig
    kind: RotatingConfig
